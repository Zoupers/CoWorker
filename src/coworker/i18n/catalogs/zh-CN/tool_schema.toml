[tool_schema.fallback]
tool = "使用 {{name}} 工具。"
parameter = "调用 {{tool}} 时使用的 {{parameter}} 参数。"

[tool_schema.description]
set_alarm = "设置在指定时间触发的闹钟，可选择重复。"
list_alarms = "列出所有尚未触发或仍会重复的闹钟。"
cancel_alarm = "按 ID 取消闹钟。"
breathe = "结束当前轮次并等待下一条输入，不设置定时唤醒。"
browser_open = "启动浏览器并导航到指定 URL，返回 session_id。"
browser_screenshot = "截取当前浏览器页面并返回 PNG 文件路径。"
browser_action = "在浏览器页面中执行点击、输入、选择或 JavaScript 操作。"
browser_get_content = "读取当前浏览器页面的文本或 HTML 内容。"
browser_close = "关闭指定浏览器会话。"
browser_view = "查看当前浏览器页面的截图。"
browser_list_sessions = "列出所有活动浏览器会话。"
bubble_spawn = "创建或续跑一个独立并行思考泡泡。"
bubble_check = "查看指定泡泡的状态和结论。"
bubble_send = "向主线程或另一个泡泡发送消息。"
bubble_cancel = "取消指定泡泡。"
bubble_list = "列出当前泡泡及其状态。"
bubble_done = "提交当前泡泡的阶段或最终结论。"
execute_code = "在隔离子进程中执行代码，可同步等待或后台运行。"
get_code_result = "读取后台代码作业的状态与输出。"
kill_code_job = "终止后台代码作业。"
communicate = "向已配置的外部参与者发送消息或附件。"
list_connections = "列出所有信道的在线连接与已知通信对象（WS/SSE 流、企微群聊/用户、Desktop actor）。"
read_file = "读取文本文件，支持分页。"
write_file = "创建、覆盖、追加或定点替换文本文件。"
list_directory = "列出目录内容。"
find_files = "按名称模式查找文件。"
grep_files = "在文件内容中搜索文本或正则表达式。"
query_memory = "查询长期记忆。"
manage_memory = "添加、更新或删除长期记忆。"
manage_pinned_context = "管理固定注入模型上下文的文本或文件。"
task_create = "创建一个 pending 状态的新任务并返回任务 ID。"
task_get = "按 ID 获取任务的完整信息。"
task_list = "列出所有未删除任务。"
task_update = "更新任务状态、描述或恢复上下文。"
get_skill = "加载指定技能的完整说明。"
switch_model = "切换当前主模型 Provider 和模型。"
get_context = "查看当前模型上下文和预算使用情况。"
restart_self = "保存状态并安全重启 Coworker。"
clear_short_term_memory = "压缩并清理短期记忆。"
sleep = "休眠指定秒数，期间仍可被新消息或闹钟唤醒。"
view_image = "加载图片并直接放入视觉模型上下文。"
visual_analyze = "在后台使用视觉模型分析图片或视频。"
search_web = "搜索互联网并返回结果摘要。"
fetch_url = "获取 URL 的正文内容。"

[tool_schema.parameter.set_alarm]
trigger_at = "首次触发时间，格式 YYYY-MM-DD HH:MM:SS。"
message = "提醒内容。"
alarm_id = "闹钟 ID；可选，用于后续取消，省略时自动生成。"
repeat_seconds = "循环间隔秒数；省略或为 0 表示一次性闹钟，正整数表示重复触发直到手动取消。"

[tool_schema.parameter.cancel_alarm]
alarm_id = "要取消的闹钟 ID。"

[tool_schema.parameter.browser_open]
url = "要打开的网页 URL。"
headless = "是否使用无头模式，默认 true。"
viewport_width = "浏览器窗口宽度（像素），默认 1280。"
viewport_height = "浏览器窗口高度（像素），默认 720。"
locale = "浏览器语言区域，如 zh-CN 或 en-US；默认跟随运行时 locale。"
timezone_id = "时区 ID，如 Asia/Shanghai 或 America/New_York。"
user_agent = "自定义 User-Agent 字符串。"
ignore_https_errors = "是否忽略 HTTPS 证书错误，默认 false。"
extra_http_headers = "附加到每个请求的 HTTP 头。"
cookies = "预设 Cookie 列表；每项需包含 name、value，以及 domain 或 url。"

[tool_schema.parameter.browser_screenshot]
session_id = "browser_open 返回的 session_id。"
full_page = "是否截取整页；默认 false，仅截取可视区域。"

[tool_schema.parameter.browser_action]
session_id = "browser_open 返回的 session_id。"
action = "要执行的操作类型。"
selector = "目标元素选择器；navigate 操作时填写 URL。"
value = "type 时为输入文本，select 时为选项值，press_key 时为按键名。"
script = "evaluate 操作要执行的 JavaScript；请避免耗时过长的代码。"

[tool_schema.parameter.browser_get_content]
session_id = "browser_open 返回的 session_id。"
fmt = "输出格式：text（默认，可见文本）或 html（原始 HTML）。"
selector = "可选 CSS 选择器，用于限定提取范围；省略时提取整页。"
start = "分页起始字符偏移量，默认 0。"

[tool_schema.parameter.browser_close]
session_id = "要关闭的 session_id。"

[tool_schema.parameter.browser_view]
session_id = "browser_open 返回的 session_id。"
full_page = "是否截取整页；默认 false，仅截取可视区域。"
full_resolution = "是否使用原始分辨率而不缩放，默认 false。"

[tool_schema.parameter.bubble_spawn]
goal = "新泡泡的具体目标；新建时必填，续跑时可作为续跑指令。"
bubble_id = "要续跑的 timeout 泡泡 ID；提供后不会新建泡泡。"
max_cycles = "新建时的最大轮次或续跑时的额外轮次；范围 1-50，累计不超过 50，默认 10。"
fresh_start = "是否仅保留 pinned context、以干净上下文开始，默认 false。"
palaces = "要挂载的记忆宫殿名称列表（见 [PALACES]）；可挂多个，留空则不挂载。"
participant_id = "该任务服务的对象 ID；绑定后，后续无歧义消息可直接转交给泡泡，泡泡只能回复该对象。"
conversation_id = "可选会话 ID；与 participant_id 一同绑定时，将转交并限制在该会话内。"
thinking = "是否启用扩展思考，默认 true；快速工具或汇总任务可设为 false。"
provider = "泡泡使用的模型 provider；留空则继承主线程。"
model = "泡泡使用的模型；留空则继承主线程。"

[tool_schema.parameter.bubble_check]
bubble_id = "泡泡 ID。"

[tool_schema.parameter.bubble_send]
target = "目标泡泡 ID，或 main 表示主线程。"
message = "要发送的消息内容。"

[tool_schema.parameter.bubble_cancel]
bubble_id = "泡泡 ID。"

[tool_schema.parameter.bubble_done]
result = "本次结论；checkpoint=true 时为阶段结论，否则为最终完整结论。"
checkpoint = "是否提交阶段检查点并继续；false（默认）表示提交最终结论并结束。"

[tool_schema.parameter.execute_code]
code = "要执行的代码。"
language = "代码语言。"
timeout = "超时秒数；省略时使用系统硬超时。"
cwd = "执行目录；默认为当前工作目录。"
output_limit = "直接返回的输出字符上限；超出部分可用 get_code_result 分页查看。"
block = "是否阻塞等待结束，默认 false；仅泡泡上下文会执行阻塞等待。"

[tool_schema.parameter.get_code_result]
job_id = "execute_code 返回的 job_id。"
offset = "分页字符偏移量，从 0 开始，默认 0。"
limit = "最多返回的字符数；传 0 表示尽量多取一页，更多内容用 offset 翻页。"

[tool_schema.parameter.kill_code_job]
job_id = "execute_code 返回的 job_id。"

[tool_schema.parameter.communicate]
participant_id = "接收方 ID。"
message = "要发送的消息内容。"
conversation_id = "同一 participant_id 下的会话 ID。"
attachments = "可选附件；具体支持情况由目标信道决定。"
  extra = "低频信道扩展；具体白名单由目标信道决定。企业微信支持 mentioned_list。"
  "extra.mentioned_list" = "企业微信 Markdown 中需要 @ 的 userid 列表。"

"attachments.item.type" = "附件类型：image 或 file。"
"attachments.item.filename" = "可选展示文件名；默认使用 path 的文件名。"
"attachments.item.media_type" = "可选 MIME 类型；默认按文件扩展名推断。"
"attachments.item.path" = "本地文件的绝对或相对路径。"

[tool_schema.parameter.read_file]
path = "文件路径。"
offset = "起始行号（从 1 开始），默认 1。"
limit = "最多读取的行数；0 或省略表示读取到文件末尾。"

[tool_schema.parameter.write_file]
path = "文件路径。"
content = "全量写入时的内容。"
append = "是否追加而不是覆盖，默认 false；仅全量写入时有效。"
old_string = "局部替换时要被替换的原始文本。"
new_string = "局部替换后的新文本。"
replace_all = "是否替换全部匹配项，默认 false。"

[tool_schema.parameter.list_directory]
path = "目录路径；默认为当前目录。"
show_hidden = "是否显示以点开头的隐藏文件，默认 false。"

[tool_schema.parameter.find_files]
pattern = "文件名 glob 模式，如 *.py 或 **/*.json。"
root = "搜索根目录；默认为当前目录。"
max_results = "最多返回的结果数，默认 50。"

[tool_schema.parameter.grep_files]
pattern = "要搜索的正则表达式或普通字符串。"
path = "搜索目标文件或目录；默认为当前目录。"
file_pattern = "只搜索匹配此 glob 的文件，如 *.py；默认搜索所有文件。"
ignore_case = "是否忽略大小写，默认 false。"
context_lines = "每个匹配行前后额外显示的行数，默认 0。"
max_matches = "最多返回的匹配数，默认 50。"

[tool_schema.parameter.query_memory]
query = "长期记忆检索关键词或描述；不可与 start/end 同时使用。"
category = "长期记忆检索时可选的分类过滤。"
tags = "长期记忆检索时可选的标签过滤；返回带任一标签的记忆。"
limit = "近期活动与长期记忆合计返回条数，默认 5，最多 10。"
start = "时间窗起点（ISO 时间）。"
end = "时间窗终点（ISO 时间）。"

[tool_schema.parameter.manage_memory]
action = "操作类型：write、update、associate 或 delete。"
content = "记忆内容；write 和 update 时必填。"
memory_id = "记忆 ID；update、associate 和 delete 时必填。"
category = "记忆分类；write 时必填。"
tags = "标签列表；write 时为初始标签，associate 时为要追加的标签。"

[tool_schema.parameter.manage_pinned_context]
action = "操作类型：pin（添加或更新）、unpin（取消）或 list（列出）。"
pin_id = "pin 的唯一语义标识；pin 和 unpin 时必填，已有 ID 会被更新。"
label = "pin 内容的标题；pin 时必填。"
content = "要 pin 的文本；pin 时与 file_path 二选一。"
file_path = "要 pin 的文件路径；每次重新注入时读取最新内容。"

[tool_schema.parameter.task_create]
description = "任务内容；若来自某个用户，请在开头注明来源，避免多用户任务混淆。"
details = "可选任务相关信息，建议使用 Markdown。"

[tool_schema.parameter.task_get]
task_id = "任务 ID。"

[tool_schema.parameter.task_update]
task_id = "要更新的任务 ID。"
status = "新状态。"
description = "新的任务描述。"
details = "任务恢复上下文；可完整替换、追加或提供单文档 unified diff。"
details_update_mode = "details 更新模式：replace、append 或 patch；省略时默认为 replace。"

[tool_schema.parameter.get_skill]
skill_name = "skill 名称，如 plan。"

[tool_schema.parameter.sleep]
seconds = "休眠秒数；支持被新消息提前唤醒，允许 0 时可等待下一次外部事件。"

[tool_schema.parameter.switch_model]
provider = "LLM 提供商实例名；由配置决定，同类型可有多个命名实例。"
model_id = "模型 ID，如 claude-sonnet-4-6；省略则使用该 provider 的默认模型。"

[tool_schema.parameter.view_image]
image_path = "图片的本地文件路径或 HTTP(S) URL。"
full_resolution = "是否使用原始分辨率而不缩放，默认 false。"

[tool_schema.parameter.visual_analyze]
media_path = "图片或视频的本地文件路径或 HTTP(S) URL。"
question = "关于图片或视频需要分析、识别或推理的具体问题。"

[tool_schema.parameter.search_web]
query = "搜索关键词。"
max_results = "最多返回的结果数，默认 5。"

[tool_schema.parameter.fetch_url]
url = "要获取的 URL。"
fmt = "输出格式：text_markdown（默认）、text_plain、text_rich 或 text（原始 HTML）。"
offset = "用于翻页读取长网页的字符偏移量，从 0 开始，默认 0。"
limit = "最多返回的字符数；更多内容用 offset 翻页。"
